import { Component } from "@angular/core";
import { ReactiveFormsModule } from "@angular/forms";

@Component({
    standalone: true,
    imports: [ReactiveFormsModule],
    templateUrl: './register.cmp.html',
    styleUrl: './register.cmp.scss'
})
export class RegisterPage {

}